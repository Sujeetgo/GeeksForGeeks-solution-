class MyQueue:
    
    #Function to push an element x in a queue.
    def __init__(self, first_in=0, last_out=0):
        self.queue = []
    def push(self, x):
         
         #add code here
         self.queue.append(x)
     
    #Function to pop an element from queue and return that element.
    def pop(self):
        if len(self.queue) ==0:
            return -1
         
         # add code here
        return self.queue.pop(0)
